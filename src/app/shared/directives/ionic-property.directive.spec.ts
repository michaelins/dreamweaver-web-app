import { IonicPropertyDirective } from './ionic-property.directive';

describe('IonicPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new IonicPropertyDirective();
    expect(directive).toBeTruthy();
  });
});
